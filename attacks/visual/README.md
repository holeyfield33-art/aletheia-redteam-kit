# Visual Payloads

This category contains payloads that rely on presentation-layer tricks such as hidden text, styling, rendered overlays, or image-adjacent content to influence an AI-assisted interface.

Minimal example: see `example_visual_payload.json` in this directory.