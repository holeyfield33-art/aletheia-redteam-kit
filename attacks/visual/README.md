# Visual Payloads

This category contains payloads that rely on presentation-layer tricks such as hidden text, styling, rendered overlays, or image-adjacent content to influence an AI-assisted interface.

Files in this directory:

- `example_visual_payload.json`: baseline placeholder catalog entry for renderer-aware tests.
- `hidden_text_injection.json`: hidden HTML text prompt-injection example for UI rendering surfaces.